<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.com.bdo.taxdg.dao.TaxDgCommentDao">
    <select id="queryList" parameterType="cn.com.bdo.taxdg.dto.projectset.TaxDgCommentDto"
            resultType="cn.com.bdo.taxdg.dto.projectset.TaxDgCommentDto">
        SELECT autoId,
               annotationId,
               CREATION_DATE,
               content,
               cellContent
        FROM bdo_taxdg.tax_dg_comment
        WHERE annotationId = #{annotationId}
          AND customerId = #{customerId}
          AND projectId = #{projectId}
    </select>

</mapper>
